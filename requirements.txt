faster-whisper>=1.1.0
pyaudio>=0.2.14
pynput>=1.7.6
numpy==2.1.0
toml>=0.10.2
# Optional: torch (for GPU)—install with: pip install torch --index-url https://download.pytorch.org/whl/cu118 (match your CUDA version)